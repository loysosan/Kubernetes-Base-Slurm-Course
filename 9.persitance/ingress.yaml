apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fileshare-ingress
spec:
  rules:
    - host: minikub-internal.ua
      http:
        paths:
          - backend:
              serviceName: fileshare
              servicePort: 80
