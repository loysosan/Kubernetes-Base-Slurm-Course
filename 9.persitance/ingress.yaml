apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: fileshare-ingress
spec:
  rules:
    - host: minikub-internal.ua
      http:
        paths:
          - backend:
              serviceName: fileshare
              servicePort: 80
